# Environment variables for Entropy Lab RS

# Bitcoin RPC Configuration (required for RPC-based scanners)
# These credentials are used for cake-wallet-rpc and android-securerandom commands
RPC_URL=http://127.0.0.1:8332
RPC_USER=your_bitcoin_rpc_username
RPC_PASS=your_bitcoin_rpc_password

# Notes:
# - Never commit actual credentials to version control
# - Copy this file to .env and fill in your actual values
# - The .env file is already in .gitignore
# - Command-line arguments take precedence over environment variables
